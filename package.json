{
  "name": "app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:blog": "node scripts/build-blog.js",
    "build:sitemap": "node scripts/generate-sitemap.js",
    "build": "npm run build:blog && npm run build:sitemap && vite build && node scripts/post-build.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "gray-matter": "^4.0.3",
    "marked": "^17.0.1",
    "sharp": "^0.34.5",
    "terser": "^5.44.1",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
