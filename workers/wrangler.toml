name = "contact-form"
main = "contact-form.js"
compatibility_date = "2024-01-01"

# You'll need to add these secrets after deployment:
# wrangler secret put RESEND_API_KEY
# wrangler secret put TO_EMAIL
# Optional (recommended):
# wrangler secret put TURNSTILE_SECRET_KEY

[vars]
# Comma-separated list of allowed browser origins (CORS)
ALLOWED_ORIGINS = "https://lonestarfauxscapes.com,https://www.lonestarfauxscapes.com"

# Email sender (should be verified in Resend for best deliverability)
FROM_NAME = "Lone Star Faux Scapes"
# FROM_EMAIL = "noreply@lonestarfauxscapes.com"
